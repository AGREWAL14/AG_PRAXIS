"""Reproduced published work, kept apart from `src/` and never modified.

Each subdirectory is one paper's model, written once and then left alone. Nothing
in here is tuned, corrected or improved; a baseline that has been quietly
repaired is not a baseline.
"""
